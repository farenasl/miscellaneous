const core = require('../../controllers/core/index');

describe('Testing controllers layer - core', () => {
    it("Core controller module is defined!", () => {
        expect(core).toBeDefined();
    });
});